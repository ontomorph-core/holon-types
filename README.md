# @holon/types

Shared TypeScript types, enums, error classes, and `ServiceResult` helpers for the HOLON clinical knowledge API.

```bash
npm install @holon/types
```

> Published mirror of the `packages/types` workspace in the Ontomorph HOLON monorepo. Source of truth lives upstream.
